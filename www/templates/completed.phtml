<section class="completed">
	<h2>Verdict</h2>
	<dl>
	<?php foreach ($state->goals as $goal): ?>
		<dt><?=$goal->description?></dt>
		<dd><?
			$answer = $goal->answer($state);
			echo $answer
				? $answer->description
				: '<pre>' . $state->facts[$goal->name] . '</pre>';
			?></dd>
	<?php endforeach ?>
	</dl>
</section>
