<section class="completed">
	<h2>Klaar!</h2>
	<dl>
	<?php foreach ($this->state->goals as $goal): ?>
		<dt><?=$goal->description?></dt>
		<dd><?=$this->state->facts[$goal->name]?></dd>
	<?php endforeach ?>
	</dl>
</section>